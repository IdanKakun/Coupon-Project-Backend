package com.idan.coupons.enums;

//An enum class that implements an errors messages.
public enum ErrorType {
    INVALID_USER,
    ILLEGAL_USER_INPUT,
    DATA_NOT_FOUND,
    OUT_OF_STOCK_OR_EXPIRED,
    CANNOT_PARSE_DATE,
    CANNOT_DELETE_EXPIRED_COUPONS,
    CANNOT_PURCHASE_COUPON
}
