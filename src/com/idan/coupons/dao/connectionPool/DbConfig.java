package com.idan.coupons.dao.connectionPool;

//A constant class that defines the connection details to the database.
public class DbConfig {

    //A static variable that implements the sqlUrl.
    public final static String sqlUrl = "jdbc:mysql://localhost:3306/coupon-project?useSSL=false&serverTimezone=UTC";

    //A static variable that implements the sqlUser.
    public final static String sqlUser = "root";

    //A static variable that implements the sqlPassword.
    public final static String sqlPassword = "id3314";

}
